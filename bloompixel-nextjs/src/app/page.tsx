'use client';

import { AppWrapper, MainLayout, MainContent } from '@/components';

export default function Home() {
  return (
    <AppWrapper>
      <MainLayout>
        <MainContent />
      </MainLayout>
    </AppWrapper>
  );
}
